name: shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: echo a string
        run: echo "hello world"
      - name: mutltline script
        run: |
          node -v
          npm -v
      - name: python command
        run: |
          import platform
          print
          (platform.processor())
      - shell: python
  run-windows-commands:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@master
      - name: Directory Powershell
        run: Get-Location
      - name: Directory Bash
        run: pwd
        shell: bash
